This contains code needed to compute the Frequentist, Assisted by Bayes intervals for logistic regression. It also contains code running experiments testing four different priors against the typical credible interval.

Note that the data_files/zip_code_db.Rdata is pulled from the R package: zipcodeR. 
